import React, { use, useMemo } from "react";
import config from "./config";

export const RunTimeTableItem = (props) => {
  return <>表格选项</>;
};

export const TableItem = {
  ...config,
  component: RunTimeTableItem,
};
